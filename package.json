{
  "name": "krm-consulting",
  "version": "1.0.0",
  "private": true,
  "description": "KRM Consulting brochure website",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check script.js && node --check test/site.test.js",
    "build": "npm test && npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
